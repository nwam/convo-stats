import {Message, MediaType} from '../src/message';
import {expect} from 'chai';
import 'mocha';
import {loadTestJson} from './testUtil';

describe('Textual Message', () => {

  it('should be a Textual Message', () => {
    expect(1).to.equal(1);
  });

});
